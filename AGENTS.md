# Agent Instructions

`CLAUDE.md` is the canonical source of truth for this repository.

Codex-specific workflow additions live in `.codex/skills/catan-workflow/SKILL.md`; they must point back to `CLAUDE.md` instead of duplicating project rules.
